/*Copamos nuestro json y agregamos el comando ctrol + shift + alt + V */
export interface RespuestaMusic {
  history: string[];
  title: string;
  art: string;
  ulistener: string;
  listeners: string;
  bitrate: string;
  djusername: string;
  djprofile: string;
}
